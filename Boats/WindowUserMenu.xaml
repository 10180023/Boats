<Window x:Class="BoatsProject.WindowUserMenu"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BoatsProject"
        mc:Ignorable="d"
        Title="WindowUserMenu" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
            <RowDefinition Height="4*"/>
        </Grid.RowDefinitions>

        <Rectangle Grid.Row="0" Fill="#FF009FDA"/>
        <Image Source="Img/logo.png" Margin="0,0,675,10"/>
        <Label Content="Меню пользователя" HorizontalAlignment="Center" Margin="279,23" VerticalAlignment="Center" FontSize="21" Foreground="White"/>
        <Button Name="NewOrder" Content="Составление заказа" HorizontalAlignment=" Center" Margin="341,113,331,0" Grid.Row="1" VerticalAlignment="Top" Width="120" Click="NewOrder_Click" Height="40" Foreground="White" Background="#FF004165"/>
    </Grid>
</Window>
